const StickyBar = () => {
  return (
    <div className="w-full bg-zinc-800 flex justify-center items-center text-white p-1 text-xs md:text-sm font-bold sticky">
      WARNING: This application is for demo purpose only
    </div>
  );
};

export default StickyBar;
